---
trigger: always_on
---

"This is a native iOS project. The host machine is Windows. Do not attempt to run local builds or use xcodebuild commands locally. All builds are remote. Prioritize SwiftUI for UI logic and use the Agent to simulate compilation logic."